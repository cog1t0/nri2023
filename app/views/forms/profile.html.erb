<div class="profile-container">
<h1>Welocome to Nomotabi 🎉</h1>
<p>大田区の魅力を発見する旅へ出掛けましょう！</p>
<p>大田区の魅力をする発見する旅へNomotabiがご案内します</p>
    <%= form_with model: @user, url: forms_create_path, method: :post, local: true do |form| %>
      <div>
        <%# <%= form.image :icon_url, value: "https://cdn2.thecatapi.com/images/0XYvRd7oD.jpg" %>
        <div class="image">
          <%= image_tag @icon.image_url, size: "150x150" %>
        </div>
      </div>
      <div class="field">
        <%= form.label :name1, 'ニックネーム' %>
        <%= form.text_field :name1, value: @user.name1, required: true %>
      </div>
      <div class="field">
        <%= form.label :profile, '自己紹介' %>
        <%= form.text_area :profile, required: true %>
      </div>
      <div class="field">
      </div>
      <div class="field">
        <%= form.label :from, '参加可能時間' %><br>
        <%= form.time_select :from, {default: Date.today + 19.hour} %>
        <%= form.time_select :to, {default: Date.today + 17.hour}  %>
      </div>
      <div>
        <%= form.hidden_field :line_id, value: @user.line_id %>
      </div>
      <div>
        <%= form.hidden_field :event_id, value: @user.event_id %>
      </div>
      <div style="margin-top: 20px;">
        <%= form.submit 'Nomotabiへ参加する👋' %>
      </div>
    <% end %>
	</form>
</div>

<!-- This is not part of Pen -->
<a class="me" href="https://codepen.io/uzcho_/pens/popular/?grid_type=list" target="_blank" style="color:#000"></a>
</div>

<style>
/* @import url('https://fonts.googleapis.com/css?family=Saira+Semi+Condensed&display=swap'); */

* {
  transition: all .5s ease;
  font-family: 'Saira Semi Condensed', sans-serif;
}

body {
  background: #dfdfdf;
}

.container {
  width: 100%;
  height: 100%;
  padding: 0 16px;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
    display: flex;
  justify-content: center;
  flex-direction: column;
  align-items: center;
}

form {
  width: 400px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

form div, form label, form input, form textarea {
  width: 100%;
}

.field:nth-of-type(2) {
  margin: 16px 0;
}

label, input, textarea {
  padding: 8px;
}

label, [placeholder] {
  color: #555;
}

label i {
  margin: 0 10px 0 0;
}

.field:focus-within label {
  color: #000;
  letter-spacing: 2px;
}

input, textarea {
  background: rgba(255, 255, 255, .5);
  border: none;
  border-radius: 4px;
  box-shadow: 0 2px 4px 0 rgba(0, 0, 0, .1);
}

input:focus, textarea:focus {
  background: rgba(255, 255, 255, 1);
  box-shadow: none;
}

textarea {
  resize: none;
}

textarea::-webkit-scrollbar {
  width: 0;
}

button {
  background: #2f4ad0;
  padding: 8px 16px;
  margin: 16px 0 50px 0;
  color: #fff;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  box-shadow: 0 2px 4px 0 rgba(0, 0, 0, .1);
}

button:hover {
  letter-spacing: 2px;
  box-shadow: none;
}

.social-media {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-around;
}

.social-media:not(:first-child) {
  font-size: 14px;
}

.social-media span {
  font-size: 16px;
}

.social-media span .fas {
  margin: 0 0 0 10px;
}

.social-media a {
  color: #000;
  font-size: 20px;
}

.image {
  display: flex;
  justify-content: center;
  align-items: center;
}

@media (max-width: 425px) {
  form {
    width: 70%;
  }
}


</style>